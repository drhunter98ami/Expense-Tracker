<UserControl x:Class="ExpenseTracker.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ExpenseTracker.Converters">
    <UserControl.Resources>
        <converters:EnglishNumberConverter x:Key="EnglishNumberConverter"/>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource AccountsTitle}"
                           Foreground="{DynamicResource TextBrush}"
                           FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="{DynamicResource AccountsSubtitle}"
                           Foreground="{DynamicResource MutedTextBrush}"
                           FontSize="14" Margin="0,6,0,0"/>
            </StackPanel>
            <Button Content="{DynamicResource AddAccountText}"
                    Command="{Binding AddAccountCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Total Assets Card -->
        <Border Grid.Row="1"
                Background="{DynamicResource PrimaryBrush}"
                CornerRadius="20"
                Padding="28"
                Margin="0,0,0,24">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" Direction="270" Opacity="0.35"
                                  ShadowDepth="6" Color="#7C3AED"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{DynamicResource TotalAssetsLabel}"
                               Foreground="{DynamicResource OnPrimaryBrush}"
                               FontSize="16" FontWeight="SemiBold" Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalAssets, Converter={StaticResource EnglishNumberConverter}, ConverterParameter=N2}"
                               Foreground="{DynamicResource OnPrimaryBrush}"
                               FontSize="38" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
                <Border Grid.Column="1"
                        Width="64" Height="64"
                        Background="#33FFFFFF"
                        CornerRadius="20"
                        VerticalAlignment="Center">
                    <TextBlock Text="💰" FontSize="28"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- Account Groups -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding AccountGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="18"
                                Margin="0,0,0,16">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18" Direction="270"
                                                  Opacity="0.1" ShadowDepth="3"
                                                  Color="#1F2937"/>
                            </Border.Effect>
                            <StackPanel>

                                <!-- Group Header -->
                                <Border Background="{DynamicResource GroupHeaderBrush}"
                                        CornerRadius="18,18,0,0"
                                        Padding="20,14">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding GroupDisplayName}"
                                                   Foreground="{DynamicResource PrimaryBrush}"
                                                   FontSize="16" FontWeight="Bold"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource GroupTotalLabel}"
                                                       Foreground="{DynamicResource MutedTextBrush}"
                                                       FontSize="14"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding FormattedGroupTotal}"
                                                       Foreground="{DynamicResource PrimaryBrush}"
                                                       FontSize="16" FontWeight="Bold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Account Rows -->
                                <ItemsControl ItemsSource="{Binding Accounts}" Margin="0,4,0,8">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="20,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Account name + label -->
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               FontSize="16" FontWeight="SemiBold"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                        <TextBlock Text="{DynamicResource CurrentBalanceLabel}"
                                                                   Foreground="{DynamicResource MutedTextBrush}"
                                                                   FontSize="13"/>
                                                        <Border Background="{DynamicResource SurfaceAltBrush}"
                                                                CornerRadius="8"
                                                                Padding="8,2"
                                                                Margin="10,0,0,0">
                                                            <TextBlock Text="{Binding Currency}"
                                                                       Foreground="{DynamicResource MutedTextBrush}"
                                                                       FontSize="12"
                                                                       FontWeight="SemiBold"/>
                                                        </Border>
                                                    </StackPanel>
                                                </StackPanel>

                                                <!-- Balance pill with per-account currency -->
                                                <Border Grid.Column="1"
                                                        CornerRadius="12"
                                                        Padding="16,10"
                                                        VerticalAlignment="Center">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#F0FDF4"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Balance}" Value="0">
                                                                    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding FormattedBalance}"
                                                               FontSize="17" FontWeight="Bold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground" Value="#16A34A"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Balance}" Value="0">
                                                                        <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Border>

                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
